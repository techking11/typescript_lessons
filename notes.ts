/**
 * Strict Null Check
 * =================================================
 * | by default all types in typescript allow null
 * | don't forget to check null
 * | run with cmd: --strictNullChecks
 * =================================================
 *
 * Non Null Assertion
 * =================================================
 * | ! don't allow null or underfined
 * | node.next!.data
 * | ts compiller don't infer that automatically
 * =================================================
 *
 * User Defined Type Guard
 * =================================================
 * | Type guard fns
 * | fn(variableName: any): variableName is string
 * |------------------------------------------------
 * | animal instanceOf Dog
 * | class Dog {} | animal: any
 * =================================================
 *
 * Mixins
 * | use by combining many classes if you need
 *
 *
 */
